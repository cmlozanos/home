{
  "name": "personal-games-home",
  "version": "1.0.0",
  "private": true,
  "description": "Development-only checks for the static family games. No production dependencies.",
  "scripts": {
    "test": "playwright test",
    "serve": "node tools/game-server.mjs",
    "icons": "node tools/game-icons.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
